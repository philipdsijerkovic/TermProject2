//- 
  Name: Philip Sijerkovic
  Date: 2/28/2025
  CSC 372-01

  This is the index.pug file for Phil's Fish Market, it has a nav bar, a search bar, a hero, and items.

doctype html
html(lang="en")
  head
    meta(charset="UTF-8")
    meta(name="viewport", content="width=device-width, initial-scale=1.0")
    title Phil's Fish Market
    link(rel="stylesheet", href="/styles.css")
    link(rel="icon", href="images/4469206.png", type="image/x-icon")
    script(src="cart.js", defer)
  
  body
    header
      h1 Phil's Fish Market
      .nav-container
        nav
          ul.nav-bar
            li
              a(href="/") Home
            li
              a(href="products") Products
            li
              a(href="cart") Cart
            li
              a(href="admin-upload") Admin View
      
      .search-bar-container
        form#search-form(action="/product-search", method="GET")
          input#search-input(type="text", name="q", placeholder="Search...")
          button(type="submit")
            img(src="images/image.png", alt="Search Icon")
    
    main
      section.hero
        img.hero-img-left(src="images/crab.gif")
        .hero-text
          h2 Welcome to Phil's Fish Market
          p "Where our seafood prices won't leave you feeling crabby."
        img.hero-img-right(src="images/crab.gif", alt="Fish Market")
      
      section.trending
        h2 Our 🔥 Hot 🔥 Products
      
      section.grid-container
        // Loop over products and display them
        each product in products
          .grid-item
            img(src=product.products_image_url, alt=product.products_name)
            h3
              a(href=`/details/${product.products_id}`) #{product.products_name}
            p $#{product.products_price}
            button.add-to-cart-btn(data-product-id=product.products_id) Add to Cart
      // Random meal API from TheMealDB 
      if randomMeal
        section.random-meal
          h2 Today's Random Recipe
          h3 #{randomMeal.strMeal}
          if randomMeal.strMealThumb
            img(src=randomMeal.strMealThumb)
          if randomMeal.strInstructions
            p #{randomMeal.strInstructions}
    
    footer
      p 2025 Phil's Fish Market. All rights reserved.
      p Contact us: contact@philsfishmarket.com